# fish_plugins — declarative plugin list for fisher
# Plugin manager: fisher (https://github.com/jorgebucaran/fisher)
# Sync: `fisher install` / `fisher update` / `fisher list` reads this file.
# Each line is a GitHub repo (or local path); fisher ignores blank lines
# and comment lines that contain spaces. Keep 14 plugins.
# Note: edc/bass 仅被 01_dev.fish 中注释掉的 uv 模板引用，保留为模板依赖；
#       oh-my-fish/plugin-foreign-env (fenv) 当前未被任何配置使用（预留）。
#       移除任一项均为需显式确认的决定。
# See also: config.fish (shell entry), conf.d/00_env.fish (PATH), conf.d/fzf.fish (keybindings).

jorgebucaran/fisher
patrickf1/fzf.fish
edc/bass
wfxr/forgit
jhillyerd/plugin-git
barnybug/docker-fish-completion
oh-my-fish/plugin-foreign-env
franciscolourenco/done
jorgebucaran/autopair.fish
meaningful-ooo/sponge
nickeb96/puffer-fish
paradox460/fzf_zoxide.fish
decors/fish-colored-man
oh-my-fish/plugin-extract
